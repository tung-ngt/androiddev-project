<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_nav_graph"
    app:startDestination="@+id/homeFrag">
    <fragment android:id="@+id/homeFrag"
        android:name="com.tungngt.dev.ui.fragments.HomeFragment">
        <action
            android:id="@+id/action_homeFrag_to_profileFrag"
            app:destination="@id/profileFrag" />
    </fragment>

    <fragment android:id="@+id/profileFrag"
        android:name="com.tungngt.dev.ui.fragments.ProfileFragment">
        <action
            android:id="@+id/action_profileFrag_to_homeFrag"
            app:destination="@id/homeFrag" />
    </fragment>

</navigation>